# Copia in .env (gitignored) e compila. DANTE carica automaticamente questo file.

# API key Anthropic per l'auth headless su server (da console.anthropic.com).
# Necessaria dove non c'è un browser per il login interattivo della CLI claude.
ANTHROPIC_API_KEY=sk-ant-...
